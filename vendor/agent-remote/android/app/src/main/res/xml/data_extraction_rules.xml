<?xml version="1.0" encoding="utf-8"?>
<!-- Profile tokens are wrapped by a non-exportable AndroidKeyStore key, so a
     transferred copy would be undecryptable anyway. Exclude them outright. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </device-transfer>
</data-extraction-rules>
